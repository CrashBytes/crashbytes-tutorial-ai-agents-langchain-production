# Core Framework
# NOTE: langchain/langchain-* are pinned to the 0.1.x line. LangChain 1.x moves the
# agent APIs used by this tutorial (langchain.agents.AgentExecutor,
# create_openai_functions_agent, langchain.schema, langchain.prompts) into
# langchain-classic and would require rewriting the tutorial. Pinned intentionally.
langchain==0.1.20
langchain-openai==0.1.7
langchain-anthropic==0.1.13
langchain-community==0.0.38
fastapi==0.136.3
uvicorn[standard]==0.49.0
# pydantic kept on 2.x (langchain 0.1.x requires pydantic <3). Updated to latest 2.x.
pydantic==2.11.9
pydantic-settings==2.7.1

# Agent Dependencies
redis==8.0.0
asyncpg==0.31.0
httpx==0.28.1
beautifulsoup4==4.15.0
python-dotenv==1.2.2

# Database
sqlalchemy==2.0.50
psycopg2-binary==2.9.12

# Monitoring & Observability
prometheus-client==0.25.0
python-json-logger==4.1.0
opentelemetry-api==1.42.1
opentelemetry-sdk==1.42.1
opentelemetry-instrumentation-fastapi==0.63b1

# Testing
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-mock==3.15.1
pytest-cov==7.1.0

# Development
black==26.5.1
ruff==0.15.16
mypy==2.1.0
pre-commit==4.6.0

# Utilities
# tenacity pinned to 8.x: langchain 0.1.x requires tenacity <9.0.0.
tenacity==8.5.0
pyyaml==6.0.3
click==8.4.1
